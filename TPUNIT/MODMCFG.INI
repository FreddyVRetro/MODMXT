{ MOD Master Defines }
{$DEFINE CGAOnly}
{$UNDEF NoMouse}

{$UNDEF MONOTONE}
{$UNDEF MODMTINY} { Remove MOD, S3M and XT }
{$DEFINE MODMLITE} { Remove 669, STM, FAR, DTM, MTM and ULT}

{$UNDEF NoSAT}

{ Mod Master SS Defines }

{ To reduce the code size you can change the Max_Channels,Max_Samples and Max_Pattern in MMSS_VAR }
{ You can alse remove the Adlib and GUS code if not needed }
{ Change must be done in MODM_SS.ASM as well               }

{$UNDEF Groupe}    { To support multiple file loading in one file       }
{$DEFINE ModeXT}   { Remove 286 code and VGA  }

{$DEFINE LOADALL} { Load all the samples (Not applicable to MMM Files )}
{$UNDEF MMMONLY}  { Load Only MMM Files      } { Save 4Kb }

{$UNDEF NoOPL}    { Add / Remove OPL2 Support} { Save 1.5Kb}
{$UNDEF NoGUS}    { Add / Remove OPL2 Support} { Save 7Kb }
{$DEFINE MIX16}   { Allow 16Bit Mixing       }

